# Production env for umnaapptst.testatozas.in
# Copy to .env on the production server (or merge these values)

# Google OAuth - MUST use production URLs
GOOGLE_CALLBACK_URL=https://umnaapptst.testatozas.in/api/auth/google/callback
FRONTEND_URL=https://umnaapptst.testatozas.in

# Also add in Google Cloud Console → APIs & Services → Credentials → Your OAuth client:
# Authorized redirect URIs: https://umnaapptst.testatozas.in/api/auth/google/callback
# Authorized JavaScript origins: https://umnaapptst.testatozas.in
