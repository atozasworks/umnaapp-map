# ============================================
# UMNAAPP Backend Environment Variables
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/umnaapp?schema=public"

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# ============================================
# Email SMTP Configuration (for OTP)
# ============================================
# Required variables for email OTP functionality

# Option 1: Use Gmail SMTP (Recommended - Best Deliverability)
# Get App Password from: https://myaccount.google.com/apppasswords
SMTP_NAME=Gmail
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_EMAIL=your-email@gmail.com
SMTP_EMAIL_PASSWORD=your-16-char-app-password

# Option 2: Use Atozas SMTP (If Gmail not working, try this)
# SMTP_NAME=atozas.com
# SMTP_SERVER=mail.atozas.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_EMAIL=no-reply@atozas.com
# SMTP_EMAIL_PASSWORD=your-email-password

# ============================================
# Google OAuth Configuration (Optional)
# ============================================
# Only required if using Google OAuth login

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# ============================================
# Frontend Configuration
# ============================================

FRONTEND_URL=http://localhost:3000

# ============================================
# Map Services (Optional overrides)
# ============================================
# SEARCH_URL=https://umnaapp.in/map/nominatim/search
# REVERSE_URL=https://umnaapp.in/map/reverse  # For taluk, district, state in search suggestions
# ROUTE_SERVICE_URL=https://umnaapp.in  # Route API base: /map/route/{profile}/{lon,lat;...}
# OSRM_URL=http://localhost:5000       # Fallback when umnaapp 502; Docker: http://osrm:5000
# NOMINATIM_URL=http://nominatim:8080        # Fallback when using Docker; /reverse used for address enrichment

# ============================================
# Notes:
# ============================================
# 1. SMTP_EMAIL_PASSWORD: Use the actual password for no-reply@atozas.com
# 2. JWT_SECRET: Generate a strong random string for production
# 3. DATABASE_URL: Update with your PostgreSQL credentials
# 4. Google OAuth: Optional - app works without it
# 5. SMTP_NAME: Used in email headers and content for branding
