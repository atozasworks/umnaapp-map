# Local development — frontend talks to local backend
VITE_API_URL=http://localhost:5000
# Map tiles - proxied via dev server to avoid CORS (umnaapp.in)
VITE_TILESERVER_URL=/map-tiles/{z}/{x}/{y}.png
