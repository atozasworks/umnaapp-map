# Local development — frontend talks to local backend
VITE_API_URL=http://localhost:5000
# India OSM tiles — same detailed style as umnaapp.in/map
VITE_TILESERVER_URL=https://umnaapp.in/tiles/{z}/{x}/{y}.png
